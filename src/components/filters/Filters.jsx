import { Form, Row, Card } from "antd";

const Filters = () => {
  return (
    <Card>
      <Form>
        <Row justify="center" wrap={false} gutter={20}>
          
        </Row>
      </Form>
    </Card>
  );
};

export default Filters;